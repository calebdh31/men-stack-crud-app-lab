<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= planet.name %></title>
</head>
    <h1><%= planet.name %></h1>
    <p>Radius: <%= planet.radius %> km</p>
    <p>Has Rings: <%= planet.hasRings ? "Yes" : "No" %></p>
    <p>Orbital Period: <%= planet.orbitalPeriod %> days</p>
    <a href="/planets">Back to All Planets</a>
    <a href="/planets/<%= planet._id %>/edit">Edit</a>
 <form action="/planets/<%= planet._id %>?_method=DELETE" method="POST" style="display:inline;">
  <button type="submit" onclick="return confirm('Are you sure you want to delete this planet?');">Delete</button>
</form>

</body>
</html>

